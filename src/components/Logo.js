import logo from '../images/Logo.png';
import '../styles/components/logo.scss';

const Logo = () => {
  return <img className='logo' src={logo} alt='logo' title='logo' />;
};

export default Logo;
